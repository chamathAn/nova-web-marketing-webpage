import styles from "./Findout.module.css"

const Findout = () => {
    return (
        <section className={styles.findout}>
            <h3 className={styles.title}>Find out more about how it work</h3>
            <button className={styles.btn}>How it works</button>
        </section>   
    )
}

export default Findout